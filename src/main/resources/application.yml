spring:
  application:
    name: Data Center Working Permit System

  # Database Configuration
  datasource:
    url: jdbc:h2:mem:workingpermitdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect

  # H2 Console
  h2:
    console:
      enabled: true
      path: /h2-console

  # Mail Configuration
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:your-email@gmail.com}
    password: ${MAIL_PASSWORD:your-app-password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  # Thymeleaf
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    check-template-location: false

# Application Configuration
app:
  jwt:
    secret: ${JWT_SECRET:mySecretKeyForWorkingPermitDataCenterSystem2025VeryLongSecretKey}
    expiration: 86400000 # 24 hours

  otp:
    length: 6
    expiration: 300000 # 5 minutes

  qrcode:
    width: 300
    height: 300

  # SMS Configuration (implement with provider like Twilio)
  sms:
    enabled: false
    provider: twilio
    account-sid: ${TWILIO_ACCOUNT_SID:}
    auth-token: ${TWILIO_AUTH_TOKEN:}
    from-number: ${TWILIO_FROM_NUMBER:}

# Server Configuration
server:
  port: 8080
  error:
    include-message: always

# Logging
logging:
  level:
    com.datacenter: DEBUG
    org.springframework.security: DEBUG
